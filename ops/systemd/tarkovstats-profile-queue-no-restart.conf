# Install as /etc/systemd/system/tarkovstats-profile-queue.service.d/retry.conf
# The wrapper retries each failing mode once; the hourly timer retries later.
# Preserve the failed service result without replaying all successful modes.
[Service]
Restart=no
